#include <stdio.h>

int outputStr(const char *str) {
  printf("%s\n", str);
  return 0;
}

int print(const int number) {
  printf("%d\n", number);
  return 0;
}

int outputChar(const char ch) {
  printf("%c\n", ch);
  return 0;
}
